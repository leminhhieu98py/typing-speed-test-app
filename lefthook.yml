pre-commit:
  parallel: true
  commands:
    linter:
      glob: '*.{ts,tsx,js,jsx}'
      run: bunx eslint {staged_files} --fix && git add {staged_files}
    formatter:
      glob: '*.{ts,tsx,js,jsx,json,md,css}'
      run: bunx prettier --write {staged_files} && git add {staged_files}
